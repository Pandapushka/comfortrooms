# Журнал разработки Comfort Rooms

Этот файл нужен, чтобы быстро восстановить контекст после перезагрузки или новой сессии.

## Основные правила проекта

- Проект называется `ComfortRooms`, публичное название — `Comfort Rooms`.
- Используем только .NET 8, целевой фреймворк `net8.0`.
- Git-действия, коммиты, пуши, merge/rebase — только после явного разрешения пользователя.
- Язык общения, комментариев и интерфейса — русский.
- Готовая HTML-страница «Изготовление люстр под заказ» является главным визуальным ориентиром.
- Карусель изображений на странице «на заказ» должна управляться через админку.
- Обычные пользователи не регистрируются и не входят на сайт. Авторизация нужна только администратору.

## Что уже сделано

- Установлен .NET SDK `8.0.421`.
- Добавлен `global.json`, закрепляющий SDK `8.0.421`.
- Создан ASP.NET Core MVC-проект `ComfortRooms`.
- Проект переведен на `net8.0`.
- Исправлены API шаблона .NET 10 на совместимые с ASP.NET Core 8.
- Добавлена Razor-страница `/sotrudnichestvo/na-zakaz` на основе готового HTML.
- Карусель на странице «на заказ» выводится через `CustomOrderPageViewModel.GalleryImages`.
- Добавлены EF Core SQLite-пакеты версии `8.0.17`.
- Добавлены сущности:
  - `AdminUser`
  - `SitePage`
  - `PageImage`
  - `LeadRequest`
- Добавлен `ComfortRoomsDbContext`.
- Добавлены сервисы:
  - `IAdminPasswordHasher` / `Pbkdf2AdminPasswordHasher`
  - `IImageStorageService` / `LocalImageStorageService`
  - `IPageContentService` / `PageContentService`
- Добавлен `DatabaseInitializer` с начальным заполнением страниц и галереи.
- В `Program.cs` подключены:
  - SQLite
  - cookie-аутентификация администратора
  - сервисы проекта
  - route для Areas
- Добавлены контроллеры админки:
  - `Areas/Admin/Controllers/AccountController.cs`
  - `Areas/Admin/Controllers/DashboardController.cs`
  - `Areas/Admin/Controllers/ImagesController.cs`
- Добавлен файл журнала `WORKLOG.md`.
- Добавлены Razor-представления админки:
  - `Areas/Admin/Views/Shared/_AdminLayout.cshtml`
  - `Areas/Admin/Views/Account/Login.cshtml`
  - `Areas/Admin/Views/Dashboard/Index.cshtml`
  - `Areas/Admin/Views/Images/Index.cshtml`
  - `Areas/Admin/Views/_ViewImports.cshtml`
- Добавлены стили админки в `wwwroot/css/site.css`.
- Пароль администратора не хранится в `appsettings*.json`; первичная учетная запись создается только если заданы `AdminSeed__Login` и `AdminSeed__Password`.
- Добавлена безопасная страница первичной настройки `/admin/setup`, доступная только пока в БД нет администратора.
- Добавлены:
  - `Areas/Admin/Controllers/SetupController.cs`
  - `Areas/Admin/Views/Setup/Index.cshtml`
  - `ViewModels/AdminSetupViewModel.cs`
- Сборка после добавления админки успешна: `0` ошибок, `0` предупреждений.
- Проверены GET-маршруты:
  - `/sotrudnichestvo/na-zakaz` — `200`
  - `/admin/setup` — `200`
  - `/admin/login` — `200`
  - `/admin` без авторизации — `302` на `/admin/login`
- Форма заявки на странице `/sotrudnichestvo/na-zakaz` переведена с JS-имитации на реальный POST.
- Добавлены:
  - `ViewModels/LeadRequestFormViewModel.cs`
  - `ViewModels/AdminLeadRequestViewModel.cs`
  - `Services/ILeadRequestService.cs`
  - `Services/LeadRequestService.cs`
  - `Areas/Admin/Controllers/LeadsController.cs`
  - `Areas/Admin/Views/Leads/Index.cshtml`
- Заявки сохраняются в таблицу `LeadRequests`.
- В админ-layout добавлена ссылка «Заявки».
- Проверен POST формы с anti-forgery token: сервер вернул `302` на `/sotrudnichestvo/na-zakaz#contact`.
- Проверена запись в SQLite: тестовая заявка появилась в `LeadRequests`.
- Собственные inline CSS/JS страницы `/sotrudnichestvo/na-zakaz` вынесены в локальные файлы:
  - `wwwroot/css/custom-order.css`
  - `wwwroot/js/custom-order.js`
- Убрано неиспользуемое модальное окно успешной заявки после перехода формы на реальный POST.
- Публичный layout получил основную навигацию:
  - Главная
  - Сотрудничество с выпадающими пунктами
  - О компании
  - Контакты
  - внешний «Интернет-магазин»
  - Админка
- Добавлен `PagesController` и простая общая view-заглушка `Views/Pages/SimplePage.cshtml` для будущих контентных страниц.
- После обновления ассетов и публичной навигации сборка успешна: `0` ошибок, `0` предупреждений.
- Проверены GET-маршруты:
  - `/` — `200`
  - `/Pages/Designers` — `200`
  - `/Pages/Shops` — `200`
  - `/Pages/Contacts` — `200`
- Добавлены красивые публичные маршруты:
  - `/o-kompanii`
  - `/kontakty`
  - `/sotrudnichestvo/dizayneram`
  - `/sotrudnichestvo/magazinam`
  - `/sotrudnichestvo/internet-magazinam`
- Публичное меню обновлено на эти маршруты.
- Страницы-заглушки получили более содержательную структуру с hero-блоком и тремя карточками.
- Проверены новые GET-маршруты:
  - `/o-kompanii` — `200`
  - `/kontakty` — `200`
  - `/sotrudnichestvo/dizayneram` — `200`
  - `/sotrudnichestvo/magazinam` — `200`
  - `/sotrudnichestvo/internet-magazinam` — `200`
- Главная страница перестала быть временной:
  - добавлен hero с изображением и CTA;
  - добавлен блок направлений;
  - добавлен блок подхода/преимуществ;
  - добавлен CTA на заявку;
  - стили добавлены в `wwwroot/css/site.css`.
- После обновления главной сборка успешна: `0` ошибок, `0` предупреждений.
- Проверен GET `/` — `200`.
- Страница `/kontakty` заменена с заглушки на полноценную контактную страницу:
  - добавлены контакты, адрес, информационные карточки и форма заявки;
  - форма сохраняет заявки через `LeadRequestService` со slug `kontakty`;
  - добавлен `ContactsPageViewModel`;
  - добавлена view `Views/Pages/Contacts.cshtml`;
  - стили добавлены в `wwwroot/css/site.css`.
- После обновления контактов сборка успешна: `0` ошибок, `0` предупреждений.
- Проверен GET `/kontakty` — `200`.
- Проверен POST формы `/kontakty` с anti-forgery token — `302` на `/kontakty#contact-form`.
- Проверена запись в SQLite: заявка появилась в `LeadRequests` со slug `kontakty`.
- В админке изображений добавлено редактирование названия и alt-текста существующих изображений.
- `IImageStorageService` получил метод `DeleteImageAsync`.
- `LocalImageStorageService` теперь удаляет локальный файл из `wwwroot/uploads` при удалении изображения из админки.
- После обновления админки изображений сборка успешна: `0` ошибок, `0` предупреждений.
- В админке изображений добавлена замена файла без удаления карточки, порядка и текстовых описаний.
- Добавлен локальный tool manifest `.config/dotnet-tools.json` с `dotnet-ef` версии `8.0.17`.
- Добавлена design-time factory `ComfortRoomsDbContextFactory`.
- Добавлена начальная EF Core миграция `InitialCreate` и snapshot модели.
- `DatabaseInitializer` переведен с `EnsureCreatedAsync` на `MigrateAsync`.
- Для старой локальной dev-БД, созданной через `EnsureCreated`, добавлена совместимость: если таблицы уже есть, а `__EFMigrationsHistory` нет, миграция не применяется повторно.
- После добавления миграций сборка успешна: `0` ошибок, `0` предупреждений.
- Проверен запуск приложения на текущей SQLite-базе.
- Проверены маршруты после миграционного слоя:
  - `/` — `200`
  - `/sotrudnichestvo/na-zakaz` — `200`
- Репозиторий привязан к `origin`: `https://github.com/Pandapushka/comfortrooms.git`.
- Ветка `main` успешно запушена в GitHub.
- Добавлен `README.md` с инструкцией запуска, структурой страниц, админкой, изображениями, БД и Git-правилами.
- Страница `/o-kompanii` заменена с заглушки на полноценную страницу:
  - hero с изображением;
  - блок принципов;
  - блок процесса;
  - CTA на изготовление под заказ и контакты.
- После обновления страницы «О компании» сборка успешна: `0` ошибок, `0` предупреждений.
- Проверен GET `/o-kompanii` — `200`.
- Страницы сотрудничества заменены с общей заглушки на полноценные страницы через общую модель и view:
  - `/sotrudnichestvo/dizayneram`
  - `/sotrudnichestvo/magazinam`
  - `/sotrudnichestvo/internet-magazinam`
- Добавлены:
  - `CooperationAudiencePageViewModel`
  - `CooperationAudienceCardViewModel`
  - `Views/Pages/CooperationAudience.cshtml`
- После обновления страниц сотрудничества сборка успешна: `0` ошибок, `0` предупреждений.
- Проверены GET-маршруты:
  - `/sotrudnichestvo/dizayneram` — `200`
  - `/sotrudnichestvo/magazinam` — `200`
  - `/sotrudnichestvo/internet-magazinam` — `200`
- Проверено применение миграций на чистой SQLite-базе `/private/tmp/comfortrooms-clean.db`.
- В чистой базе создана запись `__EFMigrationsHistory`: `20260530192000_InitialCreate | 8.0.17`.
- Seed на чистой базе создал 8 страниц и 4 изображения.
- Проверен GET `/` на чистой базе — `200`.
- Добавлена страница `/sotrudnichestvo/opt` для оптовых партнеров через общую view `CooperationAudience`.
- Публичное меню пополнено пунктом «Оптовым партнерам».
- README обновлен новым маршрутом.
- После добавления оптовой страницы сборка успешна: `0` ошибок, `0` предупреждений.
- Проверен GET `/sotrudnichestvo/opt` — `200`.
- Добавлена страница `/sotrudnichestvo/roznitsa` для розничных клиентов через общую view `CooperationAudience`.
- Публичное меню пополнено пунктом «Розничным клиентам».
- README обновлен новым маршрутом.
- После добавления розничной страницы сборка успешна: `0` ошибок, `0` предупреждений.
- Проверен GET `/sotrudnichestvo/roznitsa` — `200`.
- Добавлена таблица `PageContentBlocks` для текстовых блоков страниц.
- Добавлена админка текстов страниц:
  - `/admin/pages/{slug}/content`
  - редактирование значения каждого текстового блока;
  - кнопки «Тексты» и «Изображения» на карточках страниц в `/admin`.
- Добавлен seed стартовых hero-заголовков и описаний для основных публичных страниц.
- Для старой локальной dev-БД добавлено создание таблицы `PageContentBlocks` без пересоздания базы.
- Публичные страницы начали читать hero-заголовки и описания из `PageContentBlocks`:
  - `/`
  - `/o-kompanii`
  - `/kontakty`
  - `/sotrudnichestvo/na-zakaz`
  - `/sotrudnichestvo/opt`
  - `/sotrudnichestvo/roznitsa`
  - `/sotrudnichestvo/dizayneram`
  - `/sotrudnichestvo/magazinam`
  - `/sotrudnichestvo/internet-magazinam`
- Перед продолжением разработки текущие изменения были закоммичены и запушены по просьбе пользователя:
  - `5c8b635 Добавлено управление текстами страниц`
- После подключения публичных страниц к текстовым блокам сборка успешна: `0` ошибок, `0` предупреждений.
- Проверены GET-маршруты после подключения текстов:
  - `/` — `200`
  - `/sotrudnichestvo/na-zakaz` — `200`
  - `/kontakty` — `200`
  - `/sotrudnichestvo/opt` — `200`
- Проверен защищенный маршрут `/admin/pages/home/content` без авторизации — `302` на логин.
- Добавлен designer-файл для миграции `AddPageContentBlocks`, чтобы комплект миграции был полным.
- Расширены редактируемые текстовые блоки:
  - главная: заголовок направлений, блок подхода, CTA;
  - «О компании»: акцент hero, принципы, процесс, CTA;
  - «Контакты»: заголовок и описание формы;
  - «Изготовление под заказ»: вводный блок, процесс, преимущества, портфолио, контактная форма;
  - страницы сотрудничества: возможности, процесс и CTA.
- Админка текстов страницы переведена на сохранение всех блоков одной кнопкой.
- После расширения текстовых блоков сборка успешна: `0` ошибок, `0` предупреждений.
- На странице `/admin/login` добавлена подсказка со ссылкой на `/admin/setup`, если в базе еще нет администратора.
- Добавлен раздел `/admin/profile` для смены пароля текущего администратора.
- В шапке админки отображается логин текущего администратора.
- Добавлен `ComfortRooms.sln` для корректной работы C# Dev Kit в VS Code.
- Добавлены настройки VS Code:
  - `.vscode/launch.json` для запуска `ComfortRooms: запуск сайта`;
  - `.vscode/tasks.json` для сборки и запуска через локальный .NET 8;
  - `.vscode/settings.json` с `dotnet.defaultSolution`;
  - `.vscode/extensions.json` с рекомендуемыми C# расширениями.
- Для Development-окружения отключен `UseHttpsRedirection`, чтобы локальный HTTP-запуск на `5055` не писал warning про неопределенный HTTPS-порт.
- Исправлена логика публичных изображений:
  - добавлен `IPageContentService.GetPageImagesAsync`;
  - главная, «О компании» и страницы сотрудничества берут первое изображение страницы из админки;
  - если в админке нет изображений, используется локальный fallback `/images/fallbacks/luxury-chandelier-interior.png`;
  - для `/sotrudnichestvo/na-zakaz` дефолтная галерея теперь живет в коде и показывается только при пустой админской галерее;
  - старые временные `image.qwenlm.ai` из dev-БД нормализуются в локальный fallback при выводе.
- Сгенерирован и добавлен локальный fallback-ассет `wwwroot/images/fallbacks/luxury-chandelier-interior.png`.
- Страница `/sotrudnichestvo/na-zakaz` переписана с внешних Tailwind/Fontsource/Lucide CDN на локальные CSS/JS:
  - `Views/Cooperation/CustomOrder.cshtml`;
  - `wwwroot/css/custom-order.css`;
  - `wwwroot/js/custom-order.js`.
- В админке текстов исправлен вывод значений в textarea без HTML-сущностей.
- Добавлен публичный маршрут `/sotrudnichestvo` с обзором направлений сотрудничества.
- В меню добавлен пункт «Все направления» в выпадающем разделе «Сотрудничество».
- Добавлены недостающие блоки из анализа первоисточника:
  - на главной: цифры доверия, дополнительные направления, проекты/партнерские материалы/отзывы;
  - на контактах: офис, склад, email-направления, бренды, каталоги, место под карту;
  - на странице «На заказ»: этапы договора, материалы и покрытия, презентация, расширенный процесс;
  - на страницах сотрудничества: практические условия для опта, розницы, дизайнеров, e-commerce и магазинов.
- Сборка после исправлений успешна: `0` ошибок, `0` предупреждений.
- Проверены маршруты:
  - `/` — `200`;
  - `/sotrudnichestvo` — `200`;
  - `/sotrudnichestvo/na-zakaz` — `200`;
  - `/sotrudnichestvo/dizayneram` — `200`;
  - `/kontakty` — `200`.
- Проверено, что HTML `/sotrudnichestvo/na-zakaz` больше не содержит `cdn.tailwindcss`, `api.fontsource`, `unpkg.com` и `image.qwenlm.ai`.
- Главная страница переведена с `ViewData` в контроллере на `HomePageViewModel`.
- Для главной добавлены view model-классы:
  - `HomePageViewModel`;
  - `HomeDirectionCardViewModel`;
  - `HomeFeatureViewModel`.
- Админка текстов получила select-поля для управляемых вариантов:
  - цвет верхней hero-надписи;
  - цвет/style hero-кнопок;
  - цвет/style CTA-кнопки.
- Для главной добавлены редактируемые блоки:
  - hero eyebrow, цвет eyebrow, заголовок, описание, тексты и стили двух кнопок;
  - eyebrow и заголовок блока направлений;
  - заголовки и описания карточек направлений;
  - eyebrow, заголовок и описание блока «Подход»;
  - заголовки и описания трех карточек подхода;
  - eyebrow, заголовок, описание, текст и стиль кнопки финального CTA.
- С карточек направлений на главной убраны видимые номера `01`, `02`, `03` и т.д.
- Добавлены CSS-варианты для управляемых цветов:
  - `text-accent-gold`;
  - `text-accent-charcoal`;
  - `text-accent-terracotta`;
  - `text-accent-sage`;
  - `button--gold`;
  - `button--sage`.
- После изменений главной сборка успешна: `0` ошибок. Есть предупреждение `NU1900` из-за недоступности `https://api.nuget.org/v3/index.json` для проверки уязвимостей пакетов.
- Сайт запущен на `http://localhost:5110`, главная `/` отвечает `200`.
- Блок главной «Проекты и партнеры» заменен на блок «Отзывы».
- Для отзывов главной добавлена отдельная сущность `HomeTestimonial` с полями:
  - заголовок;
  - текст;
  - автор;
  - отдельное изображение;
  - alt-текст;
  - порядок;
  - публикация/скрытие.
- Добавлен админский раздел `/admin/home/testimonials`:
  - добавление отзыва с изображением;
  - редактирование текста, автора, alt-текста;
  - замена изображения;
  - публикация/скрытие;
  - удаление;
  - изменение порядка.
- На главной отзывы выводятся каруселью, при этом каждая карточка использует собственное изображение.
- Добавлена миграция `AddHomeTestimonials` и fallback-создание таблицы для старой локальной dev-БД.
- В `PageContentBlocks` для главной добавлены управляемые тексты блока отзывов:
  - `testimonials-eyebrow`;
  - `testimonials-title`.
- После добавления отзывов сборка успешна: `0` ошибок. Предупреждение `NU1900` остается только из-за недоступного NuGet vulnerability feed.
- Исправлено поведение отзывов на главной:
  - если опубликованных отзывов до `3` включительно, блок выводится обычной сеткой без карусели;
  - если отзывов больше `3`, включаются кнопки, точки и JS-карусель;
  - точки карусели подстраиваются под фактическое число видимых карточек на desktop/mobile;
  - карточка отзыва вынесена в partial `_HomeTestimonialCard`.
- Админка отзывов уточнена:
  - добавление нового отзыва требует отдельную картинку;
  - замена картинки принимает необязательный nullable file input в контроллере и показывает понятную ошибку, если файл не выбран;
  - в тексте админки явно указано, что карусель появляется только при количестве отзывов больше `3`.
- Сидер больше не добавляет дефолтные отзывы в таблицу `HomeTestimonials`; дефолтные отзывы остаются только публичным fallback, если админка пустая.
- Исправлен слой публичного выпадающего меню «Сотрудничество»: navbar/dropdown получили высокий `z-index`, чтобы hero-изображение главной не перекрывало меню.
- На дашборде админки действия главной страницы собраны внутри карточки «Главная страница»:
  - `Тексты`;
  - `Изображения`;
  - `Отзывы`.
- Для главной страницы добавлены новые управляемые настройки оформления:
  - фон hero, направлений, подхода, отзывов и CTA;
  - цвет заголовков, верхних надписей и описаний ключевых блоков;
  - расширенная палитра из `12` вариантов для цветов текста, фонов и кнопок.
- Сборка после расширения кастомизации успешна: `0` ошибок. Предупреждение `NU1900` остается из-за недоступного NuGet vulnerability feed.
- Перед переработкой админки текстов сделан локальный коммит без пуша:
  - `3f60bb2 Улучшена кастомизация главной и отзывы`.
- Админка текстов перестроена из плоского списка в группы:
  - связанные поля текста, цвета текста и стиля кнопки показываются рядом;
  - фон блока редактируется отдельно в заголовке группы;
  - добавлены группы Hero, Направления, Подход, Отзывы, CTA и дополнительные секции для других страниц.
- Сборка после группировки админки текстов успешна: `0` ошибок. Предупреждение `NU1900` остается из-за недоступного NuGet vulnerability feed.
- Исправлено применение фона Hero на главной:
  - старый светлый gradient-слой `home-hero::before` больше не перекрывает выбранный фон;
  - все `surface-*` варианты задают `--hero-surface-rgb`;
  - desktop и mobile overlay используют выбранный фон блока.
- Блок «Цифры доверия» на главной переведен из статического HTML в редактируемую модель:
  - верхняя надпись, заголовок, фон блока и цвета редактируются из админки;
  - четыре показателя и их описания редактируются из админки.
- Добавлен общий фон главной страницы `page-background`, который управляет пространством вокруг отдельных блоков.
- Секции главной унифицированы по ширине, чтобы цветные фоны Hero, цифр, направлений, подхода, отзывов и CTA визуально были одинакового размера.
- Исправлено фактическое применение общего фона главной:
  - `.home-page` теперь выходит за пределы Bootstrap `.container` на всю ширину viewport;
  - боковые области страницы получают выбранный в админке фон, например `surface-ink`.
- Исправлена инициализация SQLite для другого компьютера/старой локальной базы:
  - после `MigrateAsync()` теперь всегда выполняется `EnsureLegacySchemaUpdatesAsync`;
  - если в базе отсутствует таблица `HomeTestimonials`, она будет создана до загрузки главной;
  - проверка таблиц переведена на параметр `@tableName`.
- Начат конструктор добавляемых блоков:
  - добавлены сущности `PageSection` и `PageSectionButton`;
  - добавлен первый шаблон `text-image`: текст, изображение и произвольное количество кнопок со ссылками;
  - поддержаны 4 варианта расположения: картинка справа, картинка слева, картинка сверху, картинка снизу;
  - добавлен админский раздел `/admin/pages/home/sections`;
  - в карточке главной страницы появилась кнопка `Блоки`;
  - блоки выводятся на главной ниже существующих секций;
  - добавлена миграция `AddPageSections` и legacy-создание таблиц `PageSections` / `PageSectionButtons`.
- Проверено:
  - сборка успешна, `0` ошибок;
  - таблицы конструктора созданы в SQLite;
  - главная `/` отвечает `200`;
  - `/admin/pages/home/sections` без авторизации редиректит на логин `302`.
- Дополнительно проверен запуск на чистой временной SQLite-базе `/private/tmp/comfortrooms-runtime-clean.db`:
  - миграция `AddPageSections` применена;
  - таблицы `HomeTestimonials`, `PageSections`, `PageSectionButtons` присутствуют;
  - приложение дошло до запуска Kestrel, затем остановилось только из-за занятого локального порта.
- Исправлены вертикальные варианты первого шаблона конструктора:
  - `image-top` и `image-bottom` больше не рендерят изображение отдельным растягивающим рядом;
  - картинка стала фоновым слоем в hero-подобном блоке той же высоты;
  - для картинки сверху добавлен нижний gradient-overlay, для картинки снизу — верхний gradient-overlay;
  - мобильные правила тоже сохраняют hero-подобный вид.
- Исправлено переполнение текста в вариантах конструктора с изображением слева/справа:
  - контентная колонка получила внутренние отступы и `min-width: 0`;
  - длинный заголовок и описание переносятся внутри блока.
- Добавлен второй шаблон конструктора блоков `cards-grid`:
  - блок состоит из верхней надписи, заголовка, описания и произвольного количества карточек;
  - каждая карточка имеет основной текст, мелкий текст и опциональный режим ссылки;
  - карточки выводятся равномерной адаптивной сеткой;
  - карточка-ссылка получает hover-эффект как у направлений;
  - публичный вывод пользовательских блоков перенесен в partial `_PageSection`.
- Админка конструктора блоков расширена:
  - при создании блока сначала выбирается шаблон;
  - выбор шаблона сделан двумя явными карточками-радиокнопками, чтобы второй шаблон был сразу виден;
  - выбор расположения изображения показывается только для шаблона `text-image`;
  - для блоков `text-image` остаются изображение и кнопки;
  - для блоков `cards-grid` появились добавление, редактирование и удаление карточек.
- Добавлен третий шаблон конструктора блоков `testimonials`:
  - отзывы теперь создаются как отдельный тип блока, а не через отдельную вкладку главной;
  - у блока есть общие настройки верхней надписи, заголовка, описания, фона и цветов;
  - внутри блока можно добавлять отзывы с фото, автором, текстом и alt-текстом;
  - поддержаны публикация/скрытие, замена изображения, удаление и сортировка отзывов;
  - публичный вывод повторяет старую логику: до 3 отзывов сетка, больше 3 отзывов карусель;
  - JS карусели обновлен для поддержки нескольких блоков отзывов на одной странице;
  - ссылки на старую отдельную вкладку отзывов убраны из админского меню и карточки главной.
- Старые отзывы из таблицы `HomeTestimonials` автоматически переносятся в первый блок `testimonials`, если такой блок еще не создан.
- База данных расширена таблицей `PageSectionTestimonials` и legacy-созданием этой таблицы для старых SQLite-баз.
- Запрос публичных блоков переведен на `AsSplitQuery()`, чтобы секции с несколькими коллекциями не собирались тяжелым single-query.
- Для запуска на этой машине заменен стандартный `WebApplication.CreateBuilder` на минимальный `CreateEmptyBuilder`, потому что стандартный builder зависал до старта приложения; Kestrel и конфигурация подключены явно.
- Добавлен четвертый шаблон конструктора блоков `map`:
  - при создании блока можно выбрать `Шаблон 4: карта Яндекс`;
  - в форме появляется поле для HTML-кода конструктора Яндекс.Карт;
  - поле по умолчанию заполнено предоставленным кодом Яндекс.Карты;
  - публичный блок выводит только карту без заголовков и лишнего текста;
  - добавлены стили контейнера карты с адаптацией iframe/ymaps по ширине секции.
- Добавлен пятый шаблон конструктора блоков `portfolio`:
  - блок повторяет логику секции «Наши работы» со страницы изготовления люстр под заказ;
  - у блока есть верхняя надпись, заголовок, описание, фон и цвета;
  - изображения портфолио управляются внутри блока: добавить, изменить название/alt, заменить файл, удалить, переместить вверх/вниз;
  - изображения хранятся в новой таблице `PageSectionPortfolioImages`;
  - публичный вывод сделан каруселью с подписями, стрелками и точками;
  - JS карусели поддерживает несколько портфолио-блоков на одной странице.
- База данных расширена таблицей `PageSectionCards`:
  - добавлена модель `PageSectionCard`;
  - обновлены `DbContext`, миграция `AddPageSections`, snapshot/designer EF и legacy-инициализация SQLite.
- Проверено:
  - `dotnet build ComfortRooms.sln` успешен, `0` ошибок;
  - предупреждений нет после пересчета restore/assets;
  - сайт запускается на `http://localhost:5110`;
  - главная отвечает `200`;
  - таблица `PageSectionTestimonials` создана;
  - старые отзывы перенесены в блок `testimonials`;
  - HTML главной содержит блок `home-testimonials--carousel`, так как отзывов больше 3.
  - после добавления шаблона карты сборка снова успешна, `0` ошибок.
  - после добавления шаблона портфолио сборка снова успешна, `0` ошибок.

## Где остановились

Сервер в текущем шаге не запускался. Последняя проверка выполнена сборкой проекта.

## Следующие шаги

1. Запустить сайт и вручную проверить создание блока `cards-grid` из админки.
2. Проверить публичный вывод карточек-ссылок и обычных карточек на главной.
3. При необходимости добавить сортировку карточек внутри второго шаблона.
4. Продолжить следующие шаблоны конструктора блоков.
